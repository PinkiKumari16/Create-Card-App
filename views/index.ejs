<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mini Project</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <h1 class="text-white text-4xl flex justify-center p-3">Task Creator App</h1>
        <div class="form p-10 text-white">
            <form action="/create" method="POST">
                <input class="block px-5 py-2 bg-zinc-800 w-full rounded-md outline-none"  type="text" placeholder="Enter the Card Title" name="title">
                <textarea class="block px-5 py-2 bg-zinc-800 w-full rounded-md outline-none resize-none mt-2"   id="description" placeholder="Enter the Card Description" name="details"></textarea>
                <input class="mt-2 bg-blue-600 px-5 py-2 rounded-md" type="submit" value="Ceate Card">
            </form>
        </div>
        <div class="tasks p-10 flex gap-3 flex-wrap">
            <% if(files.length > 0){ %>
                <% files.forEach((file) => { %>
                    <div class="task w-60 px-3 py-4 rounded-md bg-zinc-800">
                        <h1 class="text-white text-2xl tracking-tighter"><%= file %></h1>
                        <div class="flex justify-between mt-3">
                            <a href="/edit/<%= file %>">Edit</a>
                            <a href="/files/<%= file %> " class="text-blue-500 inline-block">Read More.</a>
                        </div>
                    </div>
                <% }) %> 
            <% } else { %>
                <h3 class="text-zinc-500">No Tasks Yet.</h3>
            <% } %>
        </div>
        
    </div>
</body>
</html>